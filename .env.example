# === Sensay API Configuration ===
SENSAY_API_KEY=your_sensay_api_key_here
SENSAY_ORG_ID=your_sensay_org_id_here
SENSAY_REPLICA_ID=your_replica_uuid_here
SENSAY_USER_ID=your_user_id_here

# === Coinbase x402 Configuration ===
X402_SECRET_KEY=your_coinbase_server_secret_key_here
X402_VERIFICATION_URL=https://api.cdp.coinbase.com/x402/verify
X402_PAYMENT_AMOUNT=0.01         # Amount in USDC (e.g., 0.01 = $0.01)
X402_ASSET=usdc                  # Payment asset (must be lowercase)
X402_CHAIN=base                  # Blockchain network (e.g., base, ethereum)
X402_PAYMENT_EXPIRY=3600         # Payment expiry time in seconds (default: 1 hour)

# === Server Configuration ===
PORT=3000  # Port for the Express server
NODE_ENV=development  # Set to 'production' in production

# === Session Configuration (if using sessions) ===
# SESSION_SECRET=your_session_secret_here

# Redis Configuration (if using Redis for session/cache)
# REDIS_URL=redis://localhost:6379
# REDIS_TTL=86400  # 24 hours in seconds

# === Logging ===
LOG_LEVEL=info  # error, warn, info, debug

# === CORS Configuration (if needed) ===
# CORS_ORIGIN=http://localhost:3000
# CORS_METHODS=GET,HEAD,PUT,PATCH,POST,DELETE

# === Rate Limiting (if implemented) ===
# RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100  # Max requests per window per IP

# === Optional: Payment Processor Webhook Secret ===
# PAYMENT_WEBHOOK_SECRET=your_webhook_secret_here
